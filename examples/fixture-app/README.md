# Phase A fixture application

This fixture represents a simple web/API/database application without executing it. Create it from the dashboard as a draft with this directory as the source. Compose inspection and runtime execution deliberately begin in Milestone 2.
