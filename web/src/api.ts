export type User={id:string;username:string;role:string}; export type App={id:string;slug:string;name:string;description:string;status:string;machineName:string;createdAt:string}; export type Job={id:string;type:string;status:string;phase:string;progress:number};
let csrf=''; export const setCSRF=(token:string)=>{csrf=token};
async function request<T>(path:string, init:RequestInit={}):Promise<T>{const response=await fetch(path,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(init.method&&init.method!=='GET'?{'X-CSRF-Token':csrf}:{}),...init.headers},...init});if(!response.ok){const body=await response.json().catch(()=>({detail:'Request failed'}));throw new Error(body.detail||'Request failed')}return response.status===204?undefined as T:response.json()}
export const api={bootstrapStatus:()=>request<{bootstrapRequired:boolean}>('/api/v1/auth/bootstrap/status'),bootstrap:(data:{token:string;username:string;passphrase:string})=>request<{user:User;csrfToken:string}>('/api/v1/auth/bootstrap',{method:'POST',body:JSON.stringify(data)}),login:(data:{username:string;passphrase:string})=>request<{user:User;csrfToken:string}>('/api/v1/auth/sessions',{method:'POST',body:JSON.stringify(data)}),me:()=>request<{user:User}>('/api/v1/auth/me'),logout:()=>request<void>('/api/v1/auth/sessions/current',{method:'DELETE'}),apps:()=>request<{items:App[]}>('/api/v1/apps'),app:(id:string)=>request<App>(`/api/v1/apps/${id}`),machines:()=>request<{items:Array<{id:string;name:string;status:string;os:string;architecture:string;hostname:string}>}>('/api/v1/machines'),doctor:()=>request<{checks:Array<{name:string;ok:boolean;detail:string}>;startupLimitation:string}>('/api/v1/system/doctor'),createApp:(data:{name:string;description:string;sourcePath:string})=>request<App>('/api/v1/apps',{method:'POST',body:JSON.stringify(data)}),inspect:(sourcePath:string)=>request<{message:string}>('/api/v1/apps/import/inspect',{method:'POST',body:JSON.stringify({sourcePath})}),action:(id:string,type:'deploy'|'start'|'stop'|'restart')=>request<{job:Job}>(`/api/v1/apps/${id}/${type==='deploy'?'deployments':type}`,{method:'POST',headers:{'Idempotency-Key':crypto.randomUUID()}})};
