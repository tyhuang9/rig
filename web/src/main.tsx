import {StrictMode} from 'react'; import {createRoot} from 'react-dom/client'; import {QueryClient,QueryClientProvider} from '@tanstack/react-query'; import {createBrowserRouter,RouterProvider} from 'react-router-dom'; import {App} from './dashboard'; import {handleDashboardCaughtError} from './root-errors'; import './styles.css';
const router=createBrowserRouter([{path:'*',element:<App/>}]);
createRoot(document.getElementById('root')!, {onCaughtError: handleDashboardCaughtError}).render(<StrictMode><QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:1,staleTime:5000}}})}><RouterProvider router={router}/></QueryClientProvider></StrictMode>);
